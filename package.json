
{
  "name": "react-native-authorize-net-acceptsdk",
  "version": "1.0.1",
  "description": "`react-native-authorize-net-acceptsdk` is a package that allows you to: Fetch token by sending `card detail` to `AcceptSDK` for iOS and Android. You need to use this token to get the values of `DATA DESCRIPTOR` and `DATA VALUE` from the token.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://github.com/tushar-systango/react-native-authorizenet#readme",
  "keywords": [
    "react-native",
    "authorize.net",
    "acceptsdk"
  ],
  "author": "Tushar S",
  "license": "MIT",
  "peerDependencies": {
    "react-native": "^0.52.0"    
  }
}
